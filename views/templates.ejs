<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Resume Builder</title>
    <link rel="stylesheet" type="text/css" href="/css/main.css"> 
    <style>
        h3{
            width:100%;
            height:50px;
            margin:0;
            text-align: center;
            padding-top:30px;
        }
        img{
            max-width:100%;
            max-height:200px;
        }
        a{
            color:white;
        }
    </style>
</head>
<body>
    <header style="width:100%;height: 50px;">
        <ul style="background-color:dodgerblue;margin:0;list-style: none;display: flex;">
            <li id="first"><a href="/templates/saved">Saved</a></li>
            <a href="/logout"><li>Logout</li></a>
        </ul>
    </header>
    
   
 
    <div class="container">
            <% for(let single of items) {%>
            <div class="card">
                    <h3><%= single.title%></h3>
                    <img src="/<%=single.imagePath%>">
                    <p><%=single.description%></p>
                    <% if(admin) {%>
                    <a href="/admin/remove-template?id=<%=single._id%>">Remove template</a>
                    <% }else { %>
                    <a href="/<%=single.templatePath%>?id=<%=single._id%>">Start editing</a>
                    <% } %>
            </div>     
             <% } %>  
    </div>
</body>
</html>